import { DebounceClickDirective } from './debounce-click.directive';

describe('DebounceClickDirective', () => {
  it('should create instance', () => {
    const d = new DebounceClickDirective();
    expect(d).toBeTruthy();
  });
});
